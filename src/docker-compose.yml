
services:
  basket.db:
    image: postgres
  catalog.db:
    image: postgres
  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog.API/Dockerfile

volumes:
  catalog_volume:
  basket_volume: